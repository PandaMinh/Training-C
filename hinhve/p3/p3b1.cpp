#include<stdio.h>

int main(){
    int hang, cot, i, j;
    scanf("%d", &n);
    for(i = hang; i >= 1; ++i){
        for(j = 1; j <= cot; ++j){
            if(j <= i){
                printf("%d", )
            }
        }
    }
    return 0;
}