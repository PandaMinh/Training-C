#include<stdio.h>
#include<string.h>

int main(){
    long long t;
    scanf("%lld ", &t);
    while(t--){
        char n[100005] = {};
        scanf(" ";)
        gets(n);
        long long n = strlen(n);
        for(long long i = 0; i < n; i++){
            
        }
    }
    return 0;
}